class DataContainer{
  private static instance: DataContainer;

  static getInstance(): DataContainer{
    if(!DataContainer.instance){
        DataContainer.instance = new DataContainer();
    }
  }

}